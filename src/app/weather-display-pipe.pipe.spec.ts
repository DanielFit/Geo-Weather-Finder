import { WeatherDisplayPipe } from './weather-display-pipe';

describe('WeatherDisplayPipePipe', () => {
  it('create an instance', () => {
    const pipe = new WeatherDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
